branches:
  only:
  - travis-test
sudo: false
git:
  quiet: true
addons:
  apt:
    packages:
      - autoconf
      - automake
      - libtool
      - pkg-config
      - curl
      - git
      - cmake
      - gcc
      - gperf
      - texinfo
      - yasm
      - nasm
      - bison
      - autogen
      - patch
  homebrew:
    packages:
      - nasm
    update: false
matrix:
  include:
    - name: "iOS LTS Build"
      language: objective-c
      osx_image: xcode7.3
      before_script:
        - rm -f ./build.log
      after_success:
        - grep -e INFO ./build.log | grep build
      after_failure:
        - tail -30f ./build.log
        - tail -30f ./src/ffmpeg/ffbuild/config.log
      script:
        - bash ./ios.sh --lts --disable-armv7 --disable-armv7s --disable-arm64 --disable-i386
    - name: "iOS LTS Debug Build"
      language: objective-c
      osx_image: xcode7.3
      before_script:
        - rm -f ./build.log
      script:
        - bash ./ios.sh --lts -d
        - grep -e INFO ./build.log | grep build