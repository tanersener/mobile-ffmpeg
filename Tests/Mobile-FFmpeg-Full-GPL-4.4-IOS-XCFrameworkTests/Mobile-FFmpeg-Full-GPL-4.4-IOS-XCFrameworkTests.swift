import XCTest
@testable import Mobile_FFmpeg

final class Mobile-FFmpeg-Full-GPL-4.4-IOS-XCFrameworkTestsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Mobile-FFmpeg-Full-GPL-4.4-IOS-XCFrameworkTests().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
