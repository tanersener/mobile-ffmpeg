import XCTest
@testable import Mobile_FFmpeg

final class Mobile_FFmpegTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Mobile_FFmpeg().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
